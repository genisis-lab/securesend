{
  "name": "securesend-signal",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object signaling server for SecureSend. Relays WebRTC/ECDH handshake messages only — never sees file contents or keys.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260604.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.98.0"
  }
}
